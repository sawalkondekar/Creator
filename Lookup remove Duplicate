items = Item [ ID != 0 ].ID.getAll(); / lookup form 
for each line_items in input.Purchase_Order_Item_Details // Main Form line item
{
	items.removeelement(line_items.Items);
}
row.Items:ui.add(items);
